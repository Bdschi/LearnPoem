# Copy this file to .env and adjust the values.
# Flask will automatically load .env when you use `flask run`.

# Path to your SQLite database (required)
POEM_DB=/path/to/your/poem.db

# Secret key for Flask sessions – change this to a long random string in production!
SECRET_KEY=change-me-to-a-long-random-string

# Flask settings
FLASK_APP=app.py
FLASK_DEBUG=1
